package main

var (
	  input string
	  d := []string{input} 
	  e := []string{input}
	  shuffled := []string{}
)
func remove(numbers []int, search int) []int {
    result := []int{}
    for _, num := range numbers {
        if num != search {
            result = append(result, num)
        }
    }
    return result
}

func Shuffle(){
	if d.len > e .len{
		shuffled = append (d[0])
		remove(d,0)
		shuffled = append(result, }//0番目を取り除く
	else d.len < e.len{
		shuffled = append (e[0})
		remove(e,1) }//同様
	

		
func main(){
	fmt.Scanln(&input)//入力
	func Shuffle//Shuffle
	fmt.Printf()//Print